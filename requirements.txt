# Core dependencies - Python 3.13 compatible versions (latest)
taipy>=3.1.0
pandas>=2.2.0
numpy>=1.26.0

# File parsing dependencies - Python 3.13 compatible
ezdxf>=1.3.0
pdfminer.six>=20221105
pdf2image>=1.17.0
pytesseract>=0.3.10
python-magic>=0.4.27
pillow>=10.1.0

# Web framework dependencies - Python 3.13 compatible
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0
python-multipart>=0.0.6

# Optional CAD processing (install separately if needed)
# freecad  # STEP file processing (only in production Docker)

# Rate limiting and utilities
slowapi>=0.1.9
weasyprint>=61.0

# Additional dependencies for Python 3.13 compatibility
sqlalchemy>=2.0.25
typing-extensions>=4.9.0
greenlet>=3.0.0